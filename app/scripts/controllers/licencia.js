'use strict';

/**
 * @ngdoc function
 * @name seedApp.controller:LicenciaCtrl
 * @description
 * # LicenciaCtrl
 * Controller of the seedApp
 */
angular.module('seedApp').controller('LicenciaCtrl', function ($scope) {
    $scope.editable = 'texto editable';
});